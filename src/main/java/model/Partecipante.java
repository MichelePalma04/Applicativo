package model;

public class Partecipante extends Utente {

    /**
     * Instantiates a new Utente.
     *
     * @param login    the login
     * @param password the password
     */
    public Partecipante(String login, String password) {
        super(login, password);
    }
}
